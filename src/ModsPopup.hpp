#pragma once

// A custom "Mods" list popup (our own, not Geode's mods-index tab). Lists every
// installed mod; each row opens that mod's settings. Reached from Options.
namespace petus {
    void openModsPopup();
}
